name = "api"
main = "src/index.ts"
compatibility_date = "2025-06-01"

# Non-secret vars — accessible via c.env in Hono / env in fetch handler
[vars]
SUPABASE_URL = "https://thekcjwcdpbyjvsyziul.supabase.co"
SUPABASE_ANON_KEY = "sb_publishable_EfsGEOhtmdU-Tdph9moXsg_eLsrbO18"
SK_API_KEY_DEV = "sk-65695ceac685489e91d2508256e5d87f"

# Secrets — set via `wrangler secret put <NAME>`
# Required:
#   wrangler secret put GROQ_API_KEY_1       # Groq API key (primary AI)
#   wrangler secret put SK_API_KEY_DEV       # DeepSeek API key (fallback AI)
#   wrangler secret put BRAVE_API_KEY        # Brave Search API key (free tier: 2k queries/month)
#                                             Get it at https://api.search.brave.com



